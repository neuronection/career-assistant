# Desktop profile extras (python -m careerassistant).
# System packages needed on Linux: libgtk-3, libwebkit2gtk-4.1 (see docs/deploy.md).
-r requirements.txt

aiosqlite>=0.20
pywebview>=5.3
# 3.58+ builds against girepository-2.0 (Ubuntu 24.04+ only); the glibc-2.35
# compat packaging (22.04 runners) builds against the 1.0 API, like study.
pygobject>=3.50,<3.51 ; sys_platform == 'linux'
pystray>=0.19
Pillow>=10.0
