[mypy]
# — the type gate grows by ratchet: packages join `files` only
# when clean, and never leave. app/services is the next candidate
# (110 errors at adoption, 2026-09-09); fix in sub-slices, then add it.
ignore_missing_imports = True
follow_imports = silent
files = app/ai, app/schemas, app/models
